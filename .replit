run = "rustup"

entrypoint = "src/main.rs"

onBoot="rustup install stable && rustup default stable"

[packager]
language = "rust"

[packager.features]
packageSearch = true

[languages.rust]
pattern = "**/*.rs"

[languages.rust.languageServer]
start = "rust-analyzer"

[nix]
channel = "unstable"